!!!
%html
  %body
    = render 'planner'
    %br
    = render "search_form"
    - if params[:search].present?
      - if @recipes.blank?
        No Results
      - else
        - @recipes.each do |recipe|
          = recipe.title
