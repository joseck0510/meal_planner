= render 'navbar'
= render "search_form"
  - if params[:search].present?
  - if @recipes.blank?
    No Results
  - else
    - @recipes.each do |recipe|
      = link_to recipe.title, recipe_path(recipe)
= render 'planner'
